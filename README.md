# MovieFlix

MovieFlix — a small movie browsing/management app that uses OMDb for movie data and MongoDB for caching. Backend is built with ASP.NET (MVVM-like structure), frontend is React + TypeScript + Vite + TailwindCSS.

## Features
- Search movies by title.
- Cache results in MongoDB
- Filters: genre, year, minimum IMDb rating
- Client-side sorting & pagination
- Movie details modal (blurred background)
- CRUD-ready backend for manual movie management